#!/usr/bin/env python3

import sys,os
from jarvis_cd.basic.kill_node import KillNode

if __name__ == '__main__':
    if len(sys.argv) < 2:
        print("USAGE: jarvis-kill [regex]")
        exit(1)
    KillNode('Kill proc', sys.argv[1]).Run()