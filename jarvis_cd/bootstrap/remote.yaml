username: cc
ssh_hosts: hostfile.txt
ssh_port: 22
ssh_keys:
  primary:
    key: scs_chameleon_pass
    key_dir: ${HOME}/.ssh
    dst_key_dir: ${HOME}/.ssh
  github:
    key_name: id_rsa
    key_dir: ${HOME}/.ssh
dep_prefix: ${HOME}
spack:
  repo: https://github.com/spack/spack
  branch: releases/v0.18
  commit: null
jarvis_cd:
  repo: https://github.com/lukemartinlogan/jarvis-cd
  name: jarvis-cd
  branch: develop
  commit: null
scs_repo:
  repo: https://github.com/lukemartinlogan/scs-repo
  name: scs-repo
  branch: master
  commit: null